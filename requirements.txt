fastapi==0.141.1
# starlette é dependência transitiva do fastapi, mas fica pinada
# explicitamente porque era ela quem carregava 7 CVEs (0.41.3): deixar
# visível aqui garante que o pip-audit e uma revisão humana enxerguem a
# versão em uso, em vez de ela mudar em silêncio junto com o fastapi.
starlette==1.3.1
uvicorn[standard]==0.34.0
sqlalchemy==2.0.36
asyncpg==0.30.0
psycopg2-binary==2.9.10
alembic==1.14.0
pydantic==2.10.3
pydantic-settings==2.6.1
redis==5.2.1
httpx==0.28.1
# PyJWT no lugar de python-jose: o python-jose exigia pyasn1<0.5.0 e
# arrastava ecdsa junto, somando 5 CVEs sem saída (as correções do pyasn1
# só existem a partir da 0.6.3). A aplicação assina com HS256, que não usa
# curva elíptica nenhuma — ver app/core/security.py.
PyJWT==2.13.0
passlib[bcrypt]==1.7.4
bcrypt==4.0.1
python-multipart==0.0.31
slowapi==0.1.9
jinja2==3.1.6
aiofiles==24.1.0
openpyxl==3.1.5
python-dotenv==1.2.2
twilio==9.4.1
bleach==6.4.0
